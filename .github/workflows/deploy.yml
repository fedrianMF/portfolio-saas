name: Deploy SaaS to Production

on:
  push:
    branches: [prod] # Cambiado de main a prod

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/proyectos/saas
            # Forzar actualización desde la rama prod
            git fetch origin prod
            git reset --hard origin/prod

            # Construir y levantar con el archivo de producción
            docker compose -f docker-compose.prod.yml up -d --build

            # Ejecutar migraciones (importante en un SaaS)
            docker exec saas_app php artisan migrate --force

            docker image prune -f
